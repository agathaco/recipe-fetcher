# Copy to .env.local and fill in.

# Neon Postgres connection string — Neon dashboard > Connection Details > "Pooled connection".
# Include ?sslmode=require.
DATABASE_URL="postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require"

# Shared secret for the proxy auth gate. Any string. Empty disables the gate
# (fine for local dev). Set this in Vercel for the deployed app to be protected.
APP_PASSWORD=""
